import { ApiProperty } from '@nestjs/swagger';

// This DTO is currently empty. If needed for specific create operations
// (e.g., manual transaction creation by an admin), properties should be added here.
// For now, it serves as a base for UpdateTransactionDto and for consistency.
export class CreateTransactionDto {}
